def show_info(bot, message):
    # Заголовок
    title = "*🎲 World Of Games Bot 🎲*\n\n"

    # Основной текст
    body = (
        "Этот бот создан для помощи любителям настольных игр.\n"
        "Он собирает статистику игр, вычисляет лидеров,\n"
        "создаёт расписание предстоящих игр и хранит всё\n"
        "в простой базе данных.\n\n"
        "⚠️ Убедительная просьба не пользоваться ботом с\n"
        "нескольких учётных записей одновременно.\n"
        "Бот пока не поддерживает многопоточность,\n"
        "что будет исправлено со временем.\n\n"
        "🔧 Некоторые функции присутствуют, но не работают —\n"
        "их мы планируем добавить в будущем.\n\n"
        "📦 Это пока что самая базовая рабочая версия."
    )

    # Объединяем заголовок и тело
    full_text = f"{title}{body}"

    # Максимальная длина одного сообщения
    max_length = 4096

    # Разбиваем текст на части
    chunks = [full_text[i:i + max_length] for i in range(0, len(full_text), max_length)]

    # Отправляем каждую часть отдельно
    for chunk in chunks:
        bot.send_message(message.chat.id, chunk, parse_mode="Markdown")
